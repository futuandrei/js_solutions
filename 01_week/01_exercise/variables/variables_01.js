// Define a variable called count with an original value of 0 and then increment it (add 1 to it) on the following line.

let count = 0;

increment = count + 1;

console.log("First task result :" + increment);

console.log("Additional try :");
while (count < 10) {
    console.log("The number is : " + count);
    count = count + 1;
}